## 2024-05-20 - Initial Setup
**Learning:** Application uses Vanilla JS without Virtual DOM. Synchronous I/O in event listeners (like saving state on keypress) blocks the main thread.
**Action:** Use debounce for high-frequency events and ensure expensive DOM manipulations are optimized.
